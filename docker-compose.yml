version: "3"
services:
  sphinx-server:
    #image: dldl/sphinx-server
    build: ./
    container_name: sphinx-server
    expose:
      - 8002
    ports:
      - "8002:8002"
    volumes:
      - ./docs/source:/docs
      #- ./docs/source:/web/docs
      #- ./docs/source:/var/www/html/docs
      #- ./docs/build/html:/var/www/html/docs
    #command:
    #  - make html
    #depends_on:
    #  - reverseproxy
    #environment:
    #   VIRTUAL_HOST: ~/docs
    #   VIRTUAL_PORT: 8002
       

#networks:
#  default:
#    external:
#      name: nginx-proxy


